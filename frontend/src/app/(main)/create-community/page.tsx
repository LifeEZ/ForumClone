import { PlaceholderView } from '@/views/PlaceholderView';

export default function CreateCommunityPage() {
  return (
    <PlaceholderView
      title="Create community"
      description="Coming in build slice 4 — create a new community at /c/your-name."
    />
  );
}
