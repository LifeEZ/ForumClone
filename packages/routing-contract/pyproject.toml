[project]
name = "hiver-routing"
version = "0.1.0"
description = "Shared gateway routing contract for Hiver — the single source of truth mapping path prefixes to downstream services"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hiver_routing"]
